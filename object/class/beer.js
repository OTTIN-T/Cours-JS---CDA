export default class Beer {
     constructor(nom, type, degre) {
       this.nom = nom;
       this.type = type;
       this.degre = degre;
     }
   
     getNom() {
       return this.nom;
     }
   }
